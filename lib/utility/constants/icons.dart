import 'package:flutter/material.dart';

///Icon which is used in the IconButton that increments the time
///inside the TimeSelector widget.
const Icon incrementTimeIcon = Icon(
  Icons.fast_forward_sharp,
  color: Colors.white,
  size: 75,
);

///Icon which is used in the IconButton that decrements the time
///inside the TimeSelector widget.
const Icon decrementTimeIcon = Icon(
  Icons.fast_rewind_sharp,
  color: Colors.white,
  size: 75,
);
